<article class="card">
    <div class="card-header">
        <div>
            {{title}}
        </div>
        <div>
            Posted by {{user.username}} on {{format_date created_at}}
        </div>
    </div>
    <div class="card-body">
        <p class="card-text"> {{post-text}}</p>
    </div>
</article>